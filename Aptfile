ffmpeg
libpulse0
libblas3
liblapack3
# WeasyPrint (HTML → PDF for canvas exports) renders text through Pango +
# HarfBuzz + Fontconfig. Modern WeasyPrint (>=53) dropped Cairo/GDK-PixBuf, so
# these are the full runtime set. The heroku-community/apt buildpack must be
# indexed BEFORE heroku/python or boot fails with "cannot load library pango".
libpango-1.0-0
libpangoft2-1.0-0
libharfbuzz0b
libharfbuzz-subset0
libffi-dev
fontconfig
# Slide-deck previews render our JSON straight to PNGs with Pillow (pure Python —
# see chat/slides/pillow_render.py), so NO system render engine is needed. The
# theme fonts (Caladea, Carlito) are the committed .ttf files under
# core/assets/fonts and are loaded by Pillow directly.
#
# LibreOffice was removed: it doesn't bootstrap under Heroku's apt buildpack (UNO
# DeploymentException — see RUNBOOK) and dropping it returns ~250 MB of slug. The
# optional SLIDE_RENDER_BACKEND=libreoffice path still exists in code for anyone
# who re-adds a working LibreOffice (e.g. via a container/heroku.yml worker).
