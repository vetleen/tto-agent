ffmpeg
libpulse0
libblas3
liblapack3
# WeasyPrint (HTML → PDF for canvas exports) renders text through Pango +
# HarfBuzz + Fontconfig. Modern WeasyPrint (>=53) dropped Cairo/GDK-PixBuf, so
# these are the full runtime set. The heroku-community/apt buildpack must be
# indexed BEFORE heroku/python or boot fails with "cannot load library pango".
libpango-1.0-0
libpangoft2-1.0-0
libharfbuzz0b
libharfbuzz-subset0
libffi-dev
fontconfig
# Slide-deck rendering (worker only): LibreOffice converts .pptx -> PDF, then
# pypdfium2 (pip) rasterizes to per-slide PNGs. Use the -impress subset (pulls
# libreoffice-core) NOT the full `libreoffice` meta-package, which blows
# Heroku's 500 MB compressed slug limit. Measure slug size on staging before
# promoting. No poppler needed (pypdfium2 does the rasterization).
libreoffice-impress
